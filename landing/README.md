# Landing

The landing page for Version 848 — the first thing visitors see.

## Structure

```text
landing/
├── main.ts    # Landing page logic
└── lib/       # Supporting library code
```

Accessible as an app within the UV7 Shell via `LandingApp.ts`.
