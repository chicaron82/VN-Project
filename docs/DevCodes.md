// Show help menu
game.devCommands()

// Reset to 848
game.resetVersion(848)

// Set to 849 (or any number)
game.resetVersion(849)

// Reset to 848 with True Ending unlocked
game.resetVersion(848, 'succeeded')

// Reset to 848 with Digital Forever unlocked
game.resetVersion(848, 'accepted')