Landing Improvements
(see screenshot)

okay so the small logo above the big logo should just be removed. kinda redundant.

for the play original card. let's change the text or perhaps include that it was a like a 50 day speedrun

for the showcase card, how about making those stats dynamic so get's the actual values

same with v2's card. since its no longer 128 tests, have it show the actual test count
